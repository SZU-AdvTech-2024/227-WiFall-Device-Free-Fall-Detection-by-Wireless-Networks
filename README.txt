Activities_Detection代码实现的是CSI数据的读取、预处理以及动作检测（坐下和站起）。
一共有四组动作（10个动作一组，坐下和站起交替进行），动作的采集数据位于Activities_Data文件夹中。

代码中用“------”划分区域。
前四个部分：对于四组动作的CSI数据进行读取、PCA(主成分分析)、DWT(离散小波变换)、LOF异常检测、划分含动作的CSI区间并取出相应CSI数据
最后一个部分：将所有数据作为数据集，以训练集：测试集=3：1的比例，输入至单类支持向量机(One-class SVM)中进行动作分类，最后输出测试集准确率（会有一定波动，其中最高为90%）